services:
  cv-gen:
    build: .
    image: cv-gen:latest
    container_name: cv-gen
    ports:
      # L'application écoute sur 80 dans le conteneur ; 8080 côté hôte évite de
      # réclamer un port privilégié.
      - "8080:80"
    restart: unless-stopped
